let ans =[1,2,3];

ans.push(2); //pushes something at the end of the array 


console.log(ans);

// pop 
let initialarray=[1,2,3];
initialarray.pop() //pops something from the end of the array
console.log(initialarray);

// pops from  the start of the array 

let thisarray=[1,2,3];

thisarray.shift(); //pops from the front

console.log(thisarray);

// pushes to the start of the array 

let thatarray=[1,2,3];

thatarray.unshift(0);

console.log(thatarray);

